name: Pull Request
on:
  pull_request:
    branches:
      - main
jobs:
  build:
    uses: ./.github/workflows/build.yaml
    with:
      version: pr-${{ github.event.pull_request.number }}
      publish: false